# Task Progress

Goal: Connect the Vite app (`http://localhost:5173`) to the backend at `http://localhost:3000` via Vite's dev proxy.

- [x] 1. Add `/api` proxy → `http://localhost:3000` in `vite.config.js` (kept for when a backend is available)
- [x] 2. Update `src/App.jsx` — remove debug `console.log`, fetch sample data from `https://dummyjson.com/users` so the frontend works standalone
- [x] 3. Start `npm run dev` from `E:\vite\vite-project` (correct directory) — Vite ready at http://localhost:5173/
- [x] 4. Confirm the app loads at `http://localhost:5173` and data is fetched without errors (uses dummyjson since no backend is running on port 3000)

